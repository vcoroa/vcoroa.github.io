# vcoroa.github.io

Currículo publicado em https://vcoroa.github.io

Gerado a partir do repositório `career-assistant` (privado), com `./build.sh --web`.
Não editar `index.html` à mão: a fonte é o Markdown em `cv/`.
