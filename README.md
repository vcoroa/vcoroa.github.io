# vcoroa.github.io

Currículo publicado em https://vcoroa.github.io

Gerado a partir do repositório `career-assistant` (privado) com `./build.sh --web --publicar`.
Não editar estes arquivos à mão: a fonte é o Markdown em `cv/`.
